{
  "name": "hyperkey-store",
  "version": "1.0.0",
  "description": "HyperKey Store - Digital Games, Top Ups & Gift Cards",
  "private": true,
  "scripts": {
    "build": "echo 'Static HTML site - no build needed'",
    "lint": "echo 'No linter configured'",
    "test": "echo 'No tests configured'"
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.0.12"
  }
}
